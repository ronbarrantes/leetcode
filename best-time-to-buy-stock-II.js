var maxProfit = function (prices) {

  let profit = 0
  let price = 0


  // if nextDay is better than the currDay
  // buy

  // if the nextDay is worse than the currDay
  // sell 
  // add the difference to the profit


  return prices

};


let price = [7, 1, 5, 3, 6, 4]
console.log(maxProfit(price))